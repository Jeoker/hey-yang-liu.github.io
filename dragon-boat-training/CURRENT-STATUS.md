# 当前进度与接续入口

> 更新：2026-09-02

这份文件只记录当前可复现状态和下一步入口，供新的开发任务快速接续。产品规则仍以[项目说明](README.md)为准，功能边界和验收顺序仍以[Epic 总览](epics/README.md)为准；这里不保存已经被替代的设计讨论。

## 已验证基线

- P0 已完成：Apps Script Web App、私有系统 Spreadsheet、个人 Coach Code 会话、受保护幂等写入和退出均已真实部署并从 GitHub Pages 验收。
- P1“建季并开放一周”的阶段范围已完成真实验收：建立赛季、绑定独立 Form／响应 Spreadsheet、自动激活、Form 提交同步、十分钟名单缓存、默认模板、私有周草稿、整周开放、私有加场和单场发布均已通过。
- 当前公开入口为 <https://jeoker.github.io/hey-yang-liu.github.io/dragon-boat-training/>，管理入口为 <https://jeoker.github.io/hey-yang-liu.github.io/dragon-boat-training/coach/>。
- 当前 Web App 使用原部署 URL 的 Version 6；服务版本为 `0.3.0-p1`，契约版本为 `2026-09-02.p1.1`。真实 Script ID、私有 Spreadsheet ID、Coach Code、会话令牌及服务端 secret 不写入仓库文档。
- P1 实现提交 `91d9831` 已推送到 `master`，对应 Pages 构建与部署成功。其后的真实验收修复和本文件更新应以当前 `master` 为准。
- 本地基线为 28 项测试通过，包含完成后的建季请求重放回归；Astro 构建、后端单文件构建和 `git diff --check` 通过。

## 真实 P1 验收记录

验收使用独立测试 Form 和响应 Spreadsheet、两名虚构队员及测试地点，不使用正式队员资料。文件标识保持在仓库外。

1. Form 与响应 Tab 的只读检查返回 0 条既有回答；初始化建立每季运营 Tab、安装表单触发器并自动把赛季设为 `OPEN` 和首页默认。
2. 两次真实 Form 提交均由安装式触发器完成，名单从 0→1→2，`roster_version` 同步从 0→1→2。
3. 同名单版本的第二个页面显示“本机缓存”且沿用原绝对到期时间；第二名队员同步后，旧缓存因版本变化提前失效并取得新快照。服务端与浏览器缓存窗口均为十分钟。
4. 保存周三 18:00–20:00、周六 10:00–12:00 的默认模板后，公开页仍为休赛状态；生成 2026-09-07 私有周草稿后，两场训练仍不可见。
5. 确认整周后公开页出现两场；保存 9 月 11 日私有加场后仍为两场；单独发布该加场后准确变为三场。
6. 使用真实 Coach Code 登录并完成以上写入，退出后刷新仍停留在登录页；公开页与 Coach 页的浏览器错误日志均为空。
7. 验收发现已完成的 `createSeason` 请求在重放时会误用完成结果作为计划结果，产生一条空 ID 草稿。缺陷已修复、清理失败测试数据并部署 Version 6；回归测试确认同一完成请求不会再创建第二行。

测试环境目前保留一个公开的 `P1 Acceptance 2026` 验收赛季、两名虚构队员和三场测试训练，便于下一阶段继续使用真实接口。测试 Form 虽可通过链接填写，但响应文件及系统数据仍为私有；切换正式运营前必须另建正式赛季和独立 Form／Spreadsheet。

## 当前开发位置

下一阶段为 P2“报名与维护”，按以下顺序推进：

1. BE-05 先实现普通及管理报名、Left／Ambient／Right 容量、候补排序、直接换侧、取消与重新报名，以及服务端截止和并发保护。
2. FE-03 接入姓名搜索、报名状态、偏好修改和取消；队员仍不登录，不在浏览器保存可编辑凭证。
3. ADM-04 接入名单改名、默认偏好、启停、报名代改和管理取消，并从公开页核对实际递补结果。
4. P2 只交付报名与维护；排座及其与报名变更的公开船位联动留到 P3，训练后 24 小时更正和历史归档留到 P3／P4。

P1 阶段验收不等于 BE-03、BE-04、FE-02、ADM-02、ADM-03 的全部工作包已经关闭。默认赛季切换、赛季日期调整、当周默认场次编辑／删除、预约开放、训练修改／取消及完整结束状态仍按各工作包继续补齐。

## 继续开发时先检查

1. 在仓库根目录运行 `git -c safe.directory=D:/agents/dev-master status --short --branch`，确认没有来自其他任务的改动。
2. 运行 `npm test`、`npm run build` 和 `npm run build:dragon-boat-backend`，确认当前基线；在受限环境中运行 Astro 时设置 `ASTRO_TELEMETRY_DISABLED=1`。
3. 从实际 Pages 登录当前测试赛季，确认公开页仍有两名测试队员和三场已发布训练，再开始 P2 接口写入。
4. 后端写入继续使用服务端权限校验、`LockService`、稳定的 `request_id`、完成结果重放和对象版本冲突；浏览器不直连 Sheet，也不保存 Google 凭证。
5. Coach Mode 对 Google 写入等待 30 秒；超过后沿用同一请求编号重试。部署后仍需同时核对服务器结果和另一端页面，不能只看按钮提示。
