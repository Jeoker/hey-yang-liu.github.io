# P3 排座与最终更正验收

> 状态：**P3 阶段验收通过**。本地 121／121 测试及前后端构建通过；后端为 Version 11／服务 `0.6.1-p3`，契约 `2026-09-02.p2.1`。登录恢复修复 `360b9a6` 已由 [Pages run 33937965621](https://github.com/jeoker/hey-yang-liu.github.io/actions/runs/33937965621) 成功部署。核心 API、页面流程和真实双窗口冲突恢复通过，用户补充确认桌面鼠标拖动成功。精确 24 小时边界按 Epic 使用可控时钟验收，不要求等待真实一天；P1 剩余范围、P4 与 P5 不随本阶段关闭。

## 验收范围

P3 验收验证同一套报名与排座状态，而不建立第二个分配算法：

- Coach 草稿不进入队员公开响应，正式发布后才出现座位、Coach 和 Steerer。
- 船位始终为 10 排、左右各 10 个物理位置；Ambient 只是一种报名偏好。
- Coach 与 Steerer 由管理端设置，同一人可以兼任，但带队人员不能占用划桨报名或船位。
- 正式发布前核对所有确认报名；明确左右偏好与船位不一致时，必须再次确认。
- 报名取消、换侧和候补递补与草稿及最新正式 revision 在同次可恢复写入中联动；未发布草稿不能混入系统 revision。
- 训练结束后停止报名和递补，在 `end_at <= now < end_at + 24 小时` 内允许最终座位更正；到精确边界后直接拒绝写入。

## 本地证据

自动测试使用可控服务器时间和内存 Google Apps Script 运行时，不等待真实训练结束。核心测试位于 [p3-seating.test.mjs](p3-seating.test.mjs)，覆盖：

- 草稿隔离、完整性校验、角色互斥、同人兼任和错侧确认门槛。
- 不可变手动 revision，以及报名取消、递补、换侧产生的系统 revision。
- Coach 草稿已经分叉时不被系统递补覆盖；旧草稿不能覆盖较新的公开 revision。
- `practice_version`、`signup_version`、`seat_plan_version`、`published_revision` 冲突。
- 已完成请求重放、发布中断恢复、取消与递补中断恢复，不重复写 revision 或审计。
- 同一队员在另一场训练的报名偏好不污染当前场错侧判断。
- 赛季已经 `COMPLETED` 时，仍可读取尚在最终更正窗口内的训练。
- `UPCOMING`／`FINAL_CORRECTION` 返回 `editable: true`，`FROZEN`／`CANCELLED` 返回 `editable: false`。
- 训练结束时报名停止；最终更正不改变报名、排队或候补；精确 24 小时边界拒绝保存、发布和撤销。
- 排座自动保存成功后，动态待排座池和 20 个船位按钮立即解除写入保护，不需要手动刷新。
- 公开正式角色只投影姓名；受 Coach session 保护的管理工作区同时返回角色 `member_id`，从正式版重置可恢复 Coach、Steerer 与船位。普通 revision 和冻结快照均覆盖这一隔离边界。
- 登录超时、不可读或不完整响应时，同 Code 在原页面内复用原请求编号；Code 输入随提交清空，重复提交受到保护。换 Code、明确拒绝或完成后使用新编号；登录已成功而赛季入口读取失败保留会话，只重新读取。

公开共享组件测试另验证 10 排船位、未发布隔离、冻结提示和晚到旧 revision 防护。执行验收前从仓库根目录运行：

```text
npm test
npm run build
npm run build:dragon-boat-backend
```

## 真实脚本的安全边界

[live-p3-acceptance.mjs](live-p3-acceptance.mjs) 不随 `npm test` 执行，也不会修改真实训练时间。运行必须同时提供当前进程环境变量 `DBT_API_URL`、`DBT_COACH_CODE`，并显式传入 `--write-test-data`：

```text
node dragon-boat-training/tests/live-p3-acceptance.mjs --write-test-data
```

脚本只允许以下隔离夹具：

- 默认公开赛季名称严格等于 `P1 Acceptance 2026`，状态为 `OPEN`。
- 公开名单严格等于文档约定的 22 个虚构成员，每个姓名只出现一次。
- 赛季仍有三场已发布训练，目标为按时间排序的最后一场，地点严格等于 `P1 Test Dock`。
- 目标场次开始时没有任何有效报名；正式座位、正式 Coach 和正式 Steerer 均为空。
- 管理草稿同样没有座位或角色，避免覆盖另一位管理人员未发布的工作。

任何条件不符都会在首次业务写入前停止。脚本不读取或输出 Google 文件 ID、Coach Code、会话令牌或服务端 secret。

## 真实 API 场景

脚本按以下顺序运行，并在每一步读取服务器状态核对：

1. 十名虚构队员以 `LEFT` 报名并确认，第十一名以 `LEFT` 进入候补。
2. 同一名未报名虚构成员同时担任 Coach 与 Steerer；提交包含全部 20 个物理槽位的草稿，其中一名 Left 队员暂放右侧。
3. 公开读取仍保持原空正式版，证明草稿没有泄露。
4. 未确认错侧差异的发布收到 `PREFERENCE_ACK_REQUIRED`；携带当前版本明确确认后，发布第一个手动 revision。
5. 管理端取消一名已坐在左侧的队员；候补按原排队信息递补，并进入被释放的同一正式船位，产生独立系统 revision。
6. 另一名已坐左侧的队员直接把偏好改为 Right；保留原排队时间和序号、清除旧正式船位，并进入待排座池。
7. Coach 把该队员放到右侧空位，保存完整草稿并发布新的手动 revision。
8. 清理阶段只取消本轮创建且 `queue_at`、`queue_sequence` 仍匹配的有效报名；随后保存并发布空 Coach、空 Steerer、空座位的正式版，最后撤销 Coach 会话。

每次可能写入业务状态的请求在网络异常或可重试错误后，都使用完全相同的 `request_id` 和参数重试。如果三次后仍无法确认结果，脚本停止全部自动清理并打印原动作及请求编号。若任何有效报名不属于本轮，或者本轮队员的排队归属发生变化，清理同样立即停止，不尝试清空场次。

## 真实 API 结果

2026-09-04，脚本在 Apps Script Version 10 的隔离夹具完成运行，退出码为 0，最终 JSON 返回 `ok=true` 并确认 22 名约定虚构成员。实际观察结果：

- 包含完整 20 个物理船位的私有草稿保存后，公开训练仍显示上一正式版本，没有泄露草稿。
- 同一名未报名成员可同时担任 Coach 与 Steerer，且不占用划桨报名或船位。
- 存在明确偏好与船位不一致时，未确认发布收到 `PREFERENCE_ACK_REQUIRED`；确认当前差异后才产生首个人工 revision。
- 取消已坐左侧的队员后，最早兼容候补按原排队信息递补，并进入释放的同一正式船位，形成系统 revision。
- 另一名左侧队员改为 Right 后保留原 `queue_at` 和 `queue_sequence`，旧正式船位被清除，该成员进入待重新排座；Coach 重排并发布后续人工 revision。
- 清理阶段取消本轮创建的全部有效报名，保存并发布空 Coach、空 Steerer 和空座位正式版，随后撤销 Coach 会话；成员仍为 22 名，历史 revision 与审计记录保留。

该脚本未修改训练时间。精确 `end_at + 24 小时` 的拒绝写入和冻结行为仍仅由本地可控时钟测试证明，不记录为真实时间验收。

## 真实页面验收

2026-09-04，在 GitHub Pages run 33837698705 成功、后端更新至 Version 11／服务 `0.6.1-p3` 后，使用正式 Pages URL 和同一 Apps Script Web App 完成以下浏览器核对：

- Coach Code 登录成功；登录后只读取管理元数据，手动打开“训练与排座”后才加载名册、报名和排座工作区。
- 管理端为一名虚构成员建立受控报名，并把同一 Alpha 同时设为 Coach 与 Steerer；正式发布后队员页面显示 revision 1、完整十排左右船位及两个带队角色。
- 发布前的私有草稿修改不影响队员页面；发布后公开页才更新到对应正式 revision。
- 在 390×844 视口使用“先点姓名、再点目标船位”完成排座，不依赖拖动。
- 修正版把草稿船位从 Left1 移到 Right1 并自动保存后，待排座池和船位控件立即重新可用，不需要手动刷新。
- 在上述 Right1 草稿提交为正式版之前点击“从正式版重置草稿”，Coach 与 Steerer 立即恢复为 Alpha，船位恢复到 Left1；队员公开页继续显示 revision 1，证明重置只改变私有草稿。
- 受保护管理投影包含恢复角色所需的 `member_id`；公开投影只显示角色姓名，不带管理角色 ID。
- 应用来源的 console 没有错误；仅观察到浏览器扩展自身的 warning，不作为应用错误。

本轮实际操作发现并修复了两个问题：

1. 保存成功响应在 `seatPendingAccepted` 清除前重绘了动态按钮，之后只更新静态控件，导致池成员和船位持续禁用。成功路径现在清除保护状态后再次统一渲染动态区域，并有并发编辑回归测试。
2. 管理工作区的 `published` 曾复用公开角色投影而缺少 `member_id`，导致“从正式版重置草稿”把 Coach／Steerer 清空。后端现在用固定的公开和管理投影入口隔离字段；公开响应没有增加角色 ID。

桌面拖拽自动化没有产生 DOM 变化，该自动化结果不算通过；桌面拖动已由下文用户人工验收补齐，390×844 点选也已通过。真实双窗口版本冲突已在下文续验中通过。精确 `end_at + 24 小时` 由本地可控时钟测试证明。

## 首次核心页面验收的清理与退出

本轮 Pages 核心验收结束后，使用归属受控的严格清理脚本核对测试状态，运行以退出码 0 完成，最终返回 `ok=true`。实际结果为：

- 唯一受控报名已经取消；管理工作区读回 20 个空草稿槽位、空 Coach／Steerer 和草稿版本 v8，正式版读回为空座位、空角色及 revision 3。
- 公开与管理端读回均通过。公开页刷新显示 0／20、Revision 3、空 Coach／Steerer 和 20 个空位；Coach 页刷新显示 0／20、草稿 v8、正式 revision 3、角色未设置和 20 个空位。
- 临时脚本会话已确认退出。随后在 Coach 页面点击退出会返回登录页，导航刷新后仍只显示登录页。

## 真实结果与剩余验收

### 2026-09-04 续验：登录恢复与最新夹具

在确认前一部署 run 33838864840 成功后，本轮从真实 Edge 页面重新读取夹具。第一场已有虚构队员 12 的 Left 报名，未按旧的零报名快照清空场次。

- 本轮仅为虚构队员 01 新增 Ambient 报名；点选 Left1 后草稿保存发生超时，通过原请求重试确认保存至 v9。公开页面仍为无角色、无座位的 revision 3，两人显示待排座，没有泄露私有船位。
- 在确认控件可用、来源和目标同时可见后，分别尝试待排座池到 Left1、Left1 到 Right1 的原生自动化拖动，均未产生 DOM 变化。点选通过不替代拖动证据；拖动由后续用户人工验收确认。
- 第二个登录窗口出现超时、不可读响应和缺少会话数据。源码检查发现登录入口没有复用超时请求编号，重复尝试会消耗服务端登录次数；另有登录后读取失败清除有效会话的问题。修复保持服务端限流不变，前端在原页面内用暂存摘要匹配重新输入的 Code，复用原编号；不完整响应继续保留原请求。登录成功后读取失败只重读。四项本地回归新增后全量为 121／121。
- 最终功能部署为 `360b9a6`／run 33937965621；部署后两个窗口均真实登录成功并显示刘阳及赛季入口。异常恢复各分支仍以本地测试为证据，没有在正式浏览器中人为伪造响应。Google 连接异常原因尚未确定。
- 本轮新增报名已取消，自动清除了该队员的私有座位。双端读回第一场 1／20、零候补，仅保留原有队员 12；草稿 v10、二十个空位、角色为空；公开正式版保持 revision 3、空角色和空座位。没有新增正式发布，也没有修改其他两场。

### 真实双窗口冲突恢复

两个已登录的 Edge 窗口读取第一场相同草稿 v10、报名 v26、正式 revision 3，不新增报名，也不操作已有队员 12。

1. A 把 Coach 设为 Alpha，自动保存成功至草稿 v11，不发布。
2. B 保持 v10，把 Steerer 设为 Beta。服务器拒绝旧版本，页面保留本地 Beta，提示版本冲突，禁止发布；不会把 A 的 Alpha 清空。
3. B 点击“刷新排座”，读到 v11，仍保留本地 Beta，出现“采用服务器草稿”，发布继续禁用。
4. B 明确采用后显示服务器的 Coach Alpha、空 Steerer，并恢复发布入口；提示本地冲突版本没有提交。
5. B 清除本轮临时 Alpha，保存至 v12；A 刷新读回同一版本。最终两个角色为空、二十个空位，公开页仍是 revision 3、空角色及空座位，报名仍仅原有队员 12。

首次有效页面会话已退出并重载核对；最终两个验收会话也均已执行退出，响应及重载复核记录见下表。早先未收到 token 的登录请求可能已生成服务器会话，按既有 TTL 失效；不能声称这些会话已逐个撤销。当前夹具统一见[当前进度](../CURRENT-STATUS.md)。

### 用户人工补验与阶段关闭

2026-09-04，用户在正式 Coach Mode 新增 Member 13 的报名，反馈代报名和移回待排座池正常；在明确询问使用鼠标拖动还是点选后，用户回复“鼠标拖动成功了”。桌面拖动因此按真实人工验收通过，不记为自动化通过，也不推断已覆盖 Safari、实体手机或其他未测试场景。

结合已有 API、页面、冲突恢复和可控时钟证据，P3 阶段验收完成。用户另提出连续排座保存等待偏长，作为后续体验改进候选记录；当前 650 毫秒自动保存策略尚未改动。用户人工测试后的最新人数、草稿版本及会话状态未重新读取，不把此前 v12／1 人／已退出快照当作当前状态，也不自动清理新增 Member 13。

| 检查 | 当前状态 | 证据 |
|---|---|---|
| 本地测试、前端构建、后端单文件构建 | 通过 | 2026-09-04：`npm test` 121／121；`npm run build` 0 条诊断并生成 3 个页面；`npm run build:dragon-boat-backend` 成功；真实脚本另经 `node --check` 通过 |
| 当前部署 | 通过 | Apps Script Version 11；health 为服务 `0.6.1-p3`、契约 `2026-09-02.p2.1`；功能部署 Pages run 33937965621 成功 |
| 真实 P3 API 脚本 | 通过 | 2026-09-04，Version 10；退出码 0、最终 `ok=true`，全部业务断言通过并完成归属受控的安全清理 |
| 队员页面与 Coach Mode 双端检查 | 通过 | 登录／按需加载、受控报名、同人双角色、草稿隔离、发布、完整十排、revision 1 保持、重置、公开／管理投影隔离及收尾读回均已实测 |
| 自动保存修复 | 通过 | Left1→Right1 自动保存后，动态池和船位控件立即解锁；回归测试同步通过 |
| 桌面拖动与窄屏点选 | 通过 | 390×844 点选通过；用户于 2026-09-04 明确确认正式页面鼠标拖动成功，属于人工验收。自动化 drag 未触发的原结果保留 |
| 公开与管理角色投影 | 通过 | 管理 `published` 提供重置所需 `member_id`；公开角色仍只有姓名，重置恢复 Alpha 两个角色与 Left1 |
| 页面 console | 通过 | 应用来源无错误；仅浏览器扩展 warning |
| 真实浏览器版本冲突 | 通过 | 两窗口 v10→v11 冲突；旧输入 Beta 保留、发布被禁用；刷新保留输入，显式采用才恢复服务器 Alpha；两端最终读回已清理的 v12，公开版不变 |
| 页面测试数据清理与退出 | 通过 | 本轮仅取消新增队员 01，保留原有队员 12；临时角色已清除，公开为 1／20、Revision 3、空角色及空座位，两管理端读回草稿 v12、空角色和 20 个空位；最终两个窗口都显示“已退出 Coach Mode”，重载后仅有登录入口，无管理工作区 |
| 精确 24 小时边界 | 本地通过，真实时间未操作 | 真实脚本不篡改训练时间；若以后遇到自然窗口再补实测 |

真实 API、部署和页面验收不能互相替代。P3 已按各层证据完成验收；真实时间二十四小时尚未操作，精确边界已按 Epic 的可控时钟标准通过本地测试。继续操作前须重新读取夹具，不能用历史收尾快照覆盖后来新增的报名。
